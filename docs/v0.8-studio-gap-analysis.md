# v0.8 Studio Gap Analysis

> Evaluated: 2026-06-23
> Comparison: Sokqa Studio production samples vs. Learning Pack Factory v0.7.0
> Mode: Analysis only — no code/README/test changes

This document compares real Sokqa Studio output samples against the Learning Pack Factory output (captured in `docs/v0.8-preflight-generation-evaluation.md`) to identify the quality gap and prioritize what the Factory needs to close it.

---

## Comparison Samples

| Source | Type | Theme | ID |
|--------|------|-------|----|
| Studio | Document | ITパスポート 情報セキュリティ | `cnt_b79522bf44_doc_01` |
| Studio | Document | 英会話入門 | `cnt_e06db49920_doc_01` |
| Studio | Quiz | 日本47都道府県 | `cnt_32805554e1_quiz_single_01` |
| Studio | Quiz | カスタマーサポート英会話 | `cnt_c60e0ff434_quiz_single_01` |
| Studio | Quiz | カスタマーサポート英会話 (variant) | `cnt_a744b5beef_quiz_single_01` |

Factory equivalents come from the v0.7 evaluation packs in `output/evaluation/`.

---

## 1. Document Structure Comparison

### 1.1 Metadata-Level Structure

| Field | Studio | Factory | Gap |
|-------|--------|---------|-----|
| `id` | `cnt_<10hex>_doc_01` | `<slug>_doc_01` | Naming convention differs, no functional gap |
| `type` | `document` | `document` | ✅ Same |
| `schemaVersion` | `1` | `1` | ✅ Same |
| `title` | `"セキュリティ基礎 1. 情報セキュリティの本質と身近な対策"` — **chapter-style, descriptive** | `"ITパスポート入門 (01)"` — generic + index | **Studio titles are content-specific and chapter-oriented** |
| `description` | Content-specific learning objective | Copies config.description verbatim | **Studio writes a fresh learning objective** |
| `language` | `ja` | `ja` | ✅ Same |
| `author` | `"Sokqa Team"` | **(absent)** | **Factory missing `author` field** |
| `globalTags` | `["セキュリティ基礎", "ITパスポート", "情報セキュリティ"]` — **3+ topic tags** | `["sokqa-learning-pack-factory"]` — single generator tag | **Studio emits topic-specific tags; Factory emits only a generator tag** |
| `assetBaseUrl` | `https://cdn.convly.jp/sokqa/.../packs/cnt_<id>` | **(absent)** | **Factory missing `assetBaseUrl` field** |

### 1.2 Document Body — Size and Structure

| Metric | Studio (IT security) | Factory (it-passport-ja) | Gap |
|--------|----------------------|--------------------------|-----|
| Documents per file | **20** | 9 | Studio ~2.2× denser |
| Avg chars per document item | ~180 chars | ~40 chars | **Studio ~4.5× richer per item** |
| Total content chars | ~3,600 | ~360 | **Studio ~10× more content** |
| Document items have `tts.text` | **Partial** — only items where TTS adds value (12/20) | All items | Studio is selective; Factory is mechanical |
| Learning flow | Intro → 3 pillars (CIA) → threats → malware → phishing → targeted → social engineering → defenses → password → MFA → updates → encryption → SSL/TLS → digital signature → policy → risk mgmt → closing | Intro → Theme → Goal → Key Points → Detail → Example → Basics → Summary → Tip | **Studio follows a real pedagogical arc; Factory is a fixed template** |

### 1.3 Content Quality Verdict

- **Studio:** Each document item is a complete, self-contained learning paragraph. Concepts build on each other (機密性 → 完全性 → 可用性 → CIA triad → threats targeting each). Real examples: bank transfer tampering, server redundancy, ransomware, phishing emails, shoulder hacking, MFA, OS updates, HTTPS.
- **Factory:** Document items are template slots filled with the theme string. No concept progression. Examples are customer-service-flavored regardless of theme.

---

## 2. Quiz Structure Comparison

### 2.1 Question Quality

| Metric | Studio (47 prefectures, 20 Qs) | Factory (japan-prefectures, 5 Qs × 3 quizzes) | Gap |
|--------|--------------------------------|------------------------------------------------|-----|
| Questions per quiz | **20** | 5 | Studio 4× more |
| Unique questions | **20/20 (100%)** | **1/5 (20%)** within a quiz; same Q repeated across quizzes | **Factory questions are 80% duplicates** |
| Domain coverage | Prefecture count, breakdown (1都1道2府43県), capitals (札幌/横浜/名古屋/神戸/仙台/高松), regions (8地方), islands, ordering | Single question: "Which choice best aligns with the theme: 日本47都道府県の場所・特徴・名産品?" | **Studio covers the topic breadth; Factory repeats one question** |
| Correct answer distribution | Spread across indices 0–3 | Always index 0 | **Factory answer always position 0** — trivially guessable |

### 2.2 Explanation Quality

| Studio | Factory |
|--------|---------|
| `"日本は1都1道2府43県の合計47都道府県で構成されています。"` — states the **fact** | `"Reason: Best matches 日本47都道府県の場所・特徴・名産品."` — restates the **theme** |
| `"本州の北東部に位置する6つの県（青森、岩手、宮城、秋田、山形、福島）を東北地方と呼びます。"` — names the prefectures | `"Reason: Best matches 日本47都道府県の場所・特徴・名産品."` — identical for every question |
| Each explanation teaches the supporting fact | Every explanation is identical |

### 2.3 Distractor Quality

| Studio | Factory |
|--------|---------|
| For "都道府県の合計はいくつ" → `["43","45","47","50"]` — **plausible nearby numbers** | `"Skip the basics of ${theme}"` — meta-pattern |
| For "唯一「都」と呼ばれる" → `["京都府","北海道","大阪府","東京都"]` — **other prefectures** | `"Apply ${theme} without checking context"` — meta-pattern |
| For "県庁所在地はどこ" → `["豊田市","名古屋市","岡崎市","一宮市"]` — **other cities in same prefecture** | `"Use unclear wording when explaining ${theme}"` — meta-pattern |
| Distractors require domain knowledge to reject | Distractors are obviously wrong to anyone |

### 2.4 CS English Quiz Comparison (two Studio variants)

The two CS English quiz samples (`cnt_c60e0ff434` and `cnt_a744b5beef`) cover the **same topic** but produce **different questions, different distractors, and different explanations**. For "お問い合わせへの感謝":
- Variant A: distractors `["Thanks for calling me.","Thank you for contacting us.","Good to see you.","Hello, what is up?"]`
- Variant B: distractors `["Give me your question.","Thank you for contacting us.","What do you want?","I am busy now."]`

**Key insight: Studio generates variety even on the same topic.** Factory cannot do this — it produces identical questions every time.

### 2.5 Quiz Metadata

| Field | Studio | Factory | Gap |
|-------|--------|---------|-----|
| `author` | `"Sokqa Team"` | (absent) | Missing |
| `globalTags` | 3 topic tags | 1 generator tag | Topic tags absent |
| `assetBaseUrl` | present | (absent) | Missing |
| `title` | `"47都道府県 理解チェック1（1章）"` — chapter-anchored | `"日本47都道府県 Quiz (01)"` — generic | Studio titles reflect chapter/section |

---

## 3. TTS Comparison

### 3.1 Multilingual / Code-Switching (CRITICAL GAP)

**Studio handles mixed Japanese + English natively** with inline language tags:

```
挨拶は時間帯によって...朝一番に誰かと会ったときは、誰もが知っている [en-US]Good morning[ja-JP] から始めましょう。
```

```
[en-US]I am sorry to hear that. [ja-JP]は、顧客がトラブルや不便に直面している...
```

The English phrase is read in English (`[en-US]`), then the explanation continues in Japanese (`[ja-JP]`). This is essential for language-learning packs.

**Factory** prefixes the **entire** TTS string with `[ja-JP]` and never code-switches. For an English conversation pack, the actual English phrase would be read with Japanese pronunciation rules — unusable.

### 3.2 Abbreviation / Reading Expansion

**Studio expands abbreviations to their spoken reading** in TTS:

| Original | Studio TTS |
|----------|------------|
| `IT` | `アイティー` |
| `ID` | `アイディー` |
| `URL` | `ユーアールエル` |
| `ECサイト` | `イーシーサイト` |
| `OS` | `オーエス` |
| `SSL/TLS` | `エスエスエル ティーエルエス` |
| `Wi-Fi` | `ワイファイ` |
| `RとL` | `アールとエル` |
| `VやB` | `ヴィーやビー` |
| `脆弱性` | `ぜいじゃくせい` |
| `漏洩` | `ろうえい` |
| `遵守` | `じゅんしゅ` |

**Factory** passes the raw text through `toTts()` unchanged — the TTS engine would receive `SSL/TLS`, `URL`, `Wi-Fi` as-is, which Japanese TTS reads poorly.

### 3.3 TTS Selectivity

**Studio does not emit TTS for every document item.** Only 12 of 20 items in the IT security doc have `tts.text`. Items without TTS are presumably visual-only (diagrams, lists, headers).

**Factory** emits TTS for every item mechanically. Combined with the double-punctuation bug (`、,`), this produces a wall of low-quality TTS.

### 3.4 TTS Punctuation

| Studio | Factory |
|--------|---------|
| Clean sentence, ends with `。` or natural punctuation | Ends with `、,` (Japanese comma + English comma) — double punctuation |

### 3.5 TTS Field Coverage

| Field | Studio | Factory |
|-------|--------|---------|
| `tts.text` (documents) | Selective, high-quality, multilingual | All items, single-language, raw |
| `tts.questionText` (quiz) | Not shown in samples (likely omitted when question is simple) | Always present, mechanical |
| `tts.choiceTexts` (quiz) | Not shown in samples | Always present, all identical pattern |
| `tts.answerText` (quiz) | Not shown | Always present |
| `tts.explanationText` (quiz) | Selective, multilingual with `[en-US]`/`[ja-JP]` | Always present, single-language |

Studio appears to **emit TTS only where it adds value** (explanations with English phrases, document narration). Factory emits TTS everywhere uniformly.

---

## 4. Studio-Specific Feature Extraction

### 4.1 Features Factory Lacks (Porting Candidates)

| # | Feature | Value | Difficulty |
|---|---------|-------|------------|
| F1 | **LLM-driven content generation improvements** (topic-aware paragraphs from theme/reference, not theme-string templates) | Game-changing | Hard — requires prompt design and output validation |
| F2 | **LLM-generated question variety** (different questions on the same topic, with different distractors and explanations) | High | Hard — requires prompt-level variation control |
| F3 | **Inline multilingual TTS** (`[en-US]...[ja-JP]...`) | Critical for language packs | Medium — string interpolation |
| F4 | **Abbreviation-to-reading expansion** (IT→アイティー, URL→ユーアールエル) | Critical for ja TTS quality | Medium — mapping table |
| F5 | **Selective TTS** (omit TTS for visual-only items) | Quality | Easy — per-item flag |
| F6 | **Topic-specific `globalTags`** | Metadata richness | Easy — derive from theme |
| F7 | **`author` field** | Schema parity | Trivial |
| F8 | **`assetBaseUrl` field** | Schema parity | Trivial |
| F9 | **Chapter-anchored titles** (`"セキュリティ基礎 1. ..."`) | Discoverability | Easy |
| F10 | **Answer index distribution** (not always 0) | Fairness | Easy — shuffle |
| F11 | **LLM-generated topic-specific distractors** (other prefectures, other cities, nearby numbers) | Critical for quiz usefulness | Hard — requires LLM to generate topic-aware content from theme/reference |
| F12 | **Learning-arc document structure** (Intro → Concepts → Threats → Defenses → Closing) | Pedagogical value | Hard — needs content design |

### 4.2 Features Factory Has That Studio Samples Don't Show

| Feature | Note |
|---------|------|
| `reference` / `source_plus` / `source_only` / `exact_text_document` modes | Factory's strength — Studio samples appear to be hand-authored |
| `profile` with 14 generation controls | Factory's strength — Studio samples don't expose a profile schema |
| Validation system (38 tests) | Factory's strength |
| ZIP export | Factory's strength |
| Preset generation controls (difficulty, quizStyle, etc.) | Factory's strength — but underutilized since output is template-based |

### 4.3 Features Studio Has That Factory Likely Doesn't Need

| Feature | Reason |
|---------|--------|
| `assetBaseUrl` pointing to a CDN | Deployment-specific; Factory's ZIP model is fine for local generation |
| `author: "Sokqa Team"` | Could be hardcoded or config-driven; minor |

---

## 5. Cross-Cutting Findings

### 5.1 The Core Gap: Content vs. Scaffolding

The fundamental difference is that **Studio generates content; Factory generates scaffolding.**

- Studio writes 20 distinct paragraphs explaining real security concepts.
- Factory writes 9 template slots filled with the theme string.

This single difference accounts for the vast majority of the quality gap. All other features (TTS quality, distractor quality, tag richness) are secondary to this.

### 5.2 The Factory's Strength Is Also Its Weakness

Factory's `profile` system (14 generation controls) is a strength because it makes generation configurable. But it is also a weakness because the controls adjust **template wording** (e.g., `difficulty: hard` adds the word "applied" to the correct answer) rather than **content substance**. The profile system has no content to act upon.

### 5.3 Reference Mode Is the Bridge

Factory's reference modes (`source_only`, `source_plus`, `exact_text_document`) are the only path to Studio-quality output today. When a real reference document is provided, Factory can produce content-rich packs. The gap is specifically in **no-reference (`mode: none`) generation**, where Factory falls back to empty templates.

---

## 6. v0.8 Priority

### v0.8

| Item | Rationale |
|------|-----------|
| **LLM Prompt Quality Improvements** | `mode: none` is weak because current prompts produce template scaffolding instead of topic-aware content. |
| **LLM-generated Question Variety** | A quiz with repeated stems has no learning value; prompts must produce distinct question angles across slots and quizzes. |
| **LLM-generated Topic-specific Distractors** | Wrong answers must be plausibly related to the theme/reference, not generic bad-approach phrases. |
| **TTS Normalization** | Current TTS output quality issues such as `、,` reduce usability even when structure validates. |
| **Inline Multilingual TTS Rules** | Language-learning packs need reliable `[en-US]...[ja-JP]` switching inside a single output. |
| **Output Quality Validation** | Stronger validation is needed so low-value repeated or generic outputs are rejected before export. |

### v0.9

| Item | Rationale |
|------|-----------|
| **Preset Profiles** | Reduce config overhead after core generation quality is improved. |
| **Config Simplification** | Simplify authoring once prompt/output quality is stable. |
| **Reference Weighting** | Improve how multiple references influence generation after baseline quality is reliable. |
| **PDF Quality Improvements** | Output format polish follows content quality. |
| **Advanced Reference Handling** | More advanced source behavior belongs after the no-reference quality gap is closed. |

---

## 7. Summary

### Studio's Strengths

1. **Generates real educational content** — paragraphs that teach concepts, not templates that mention the theme.
2. **Quiz questions are distinct and domain-specific** — 20 unique questions covering topic breadth, with plausible domain distractors.
3. **TTS is multilingual and abbreviation-aware** — code-switches `[en-US]`/`[ja-JP]` and expands `IT→アイティー`.
4. **Metadata is rich** — topic tags, author, assetBaseUrl, chapter-anchored titles.
5. **Produces variety on the same topic** — two CS English quiz variants differ throughout.

### Factory's Weaknesses

1. **No-reference mode produces empty scaffolding** — theme string inserted into templates, no real content.
2. **Quiz questions are duplicates** — 80%+ repetition within and across quizzes.
3. **Distractors are meta-patterns** — obviously wrong, with no topic-aware factual plausibility.
4. **TTS is single-language and raw** — no code-switching, no abbreviation expansion, double punctuation `、,`.
5. **Answer always at index 0** — trivially guessable.
6. **Metadata is thin** — single generator tag, no topic tags, no author/assetBaseUrl.

### The Path to Parity

The gap is large but **structurally clear**. The Factory's config/profile/validation/ZIP infrastructure is solid. What it lacks is **reliable LLM-driven generation quality in no-reference mode**. Once prompt quality, question variety, distractor quality, and validation improve together, TTS and metadata enhancements become polish on meaningful output rather than polish on empty templates.

**Recommended v0.8 focus: LLM Prompt Quality Improvements + LLM-generated Question Variety + LLM-generated Topic-specific Distractors + TTS Normalization + Inline Multilingual TTS Rules + Output Quality Validation.** Defer preset/config/reference/PDF improvements to v0.9.

---

## Appendix: Studio Sample Inventory

| Sample | Type | Theme | Questions/Docs | Notable Feature |
|--------|------|-------|----------------|-----------------|
| `cnt_b79522bf44_doc_01` | Document | 情報セキュリティ | 20 docs | CIA triad arc, abbreviation TTS |
| `cnt_e06db49920_doc_01` | Document | 英会話入門 | 20 docs | `[en-US]` code-switching throughout |
| `cnt_32805554e1_quiz_single_01` | Quiz | 47都道府県 | 20 Qs | Domain distractors (prefectures, cities, numbers) |
| `cnt_c60e0ff434_quiz_single_01` | Quiz | CS英会話 | 20 Qs | Multilingual explanation TTS |
| `cnt_a744b5beef_quiz_single_01` | Quiz | CS英会話 (variant) | 20 Qs | Same topic, fully different questions/distractors |
