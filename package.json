{
  "name": "sokqa-pack-factory",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "generate": "tsx scripts/generate-learning-pack.ts",
    "generate:example": "tsx scripts/generate-learning-pack.ts --input examples/customer-service.txt --id customer-service-pack --title \"Customer Service Training\"",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@types/node": "20.14.10",
    "tsx": "^4.20.6",
    "typescript": "5.5.4",
    "vitest": "^3.2.4"
  }
}
